# Portfolio Mini Mobile App

Minimal Expo React Native application for tracking an investment portfolio.

## Local development

```sh
pnpm install
pnpm start
```

Press `a` in the Expo terminal to open the app on an available Android emulator.

Run the TypeScript check with:

```sh
pnpm typecheck
```
